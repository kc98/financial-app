import { StyleSheet } from "react-native";

const alignments = StyleSheet.create({
  center: {
    alignSelf: "center",
    justifyContent: "center",
    alignItems: "center",
  },
});

export { alignments };
